{
  "name": "url-shortner",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "db:studio": "bunx drizzle-kit studio",
    "db:generate": "bunx @better-auth/cli generate   --config ./src/lib/auth.ts   --output ./src/db/schema.ts",
    "db:push": "bunx drizzle-kit push",
    "dev": "bun --watch run index.ts",
    "start": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "better-auth": "^1.4.15",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.4",
    "postgres": "^3.4.8"
  }
}
